[build]
builder = "nixpacks"

[phases.setup]
nixPkgs = ["python311Full", "ffmpeg", "gcc"]

[phases.install]
cmds = ["pip3 install --upgrade pip", "pip3 install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT:-5000}"
