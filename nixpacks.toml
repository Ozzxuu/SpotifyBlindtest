[build]
builder = "nixpacks"

[phases.setup]
nixPkgs = ["python311", "pip", "ffmpeg", "gcc"]

[phases.install]
cmds = ["pip install --upgrade pip", "pip install -r requirements.txt"]

[start]
cmd = "gunicorn app:app --bind 0.0.0.0:${PORT:-5000}"
